#include<stdio.h>
#include<conio.h>
void main()
{
	int number;
	printf("Enter the value :");
	scanf("%d",&number);
	if(number>0)
	{
		printf("Value have a Positive");
	}
	else
	{
		printf("Value have a Negative");
	}
}