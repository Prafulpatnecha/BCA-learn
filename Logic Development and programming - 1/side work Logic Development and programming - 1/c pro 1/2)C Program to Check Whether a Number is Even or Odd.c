#include<stdio.h>
#include<conio.h>
void main()
{
	//2)C Program to Check Whether a Number is Even or Odd.
	int a;
	//enter the value of number.
	printf("Odd & Even Enter the value find :");
	scanf("%d",&a);
	//odd and even find formula (n%2==0).
	if(a%2==0)
	{
		printf("this value is evan");
	}
	else
	{
		printf("this value is odd");
	}
}
